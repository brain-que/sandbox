# AGENTS.md

This repo is a sandbox target for the autodev loop.

- Stack: Python. Run tests with `pytest` (from the repo root).
- Keep changes small and single-purpose; add a test for any new function.
- Open a PR; never push to main. A human reviews and merges.
